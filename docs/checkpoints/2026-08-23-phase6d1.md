# Checkpoint — 2026-08-23 — Phase 6D.1 App packaging

## Status

- Phase 6C: COMPLETE.
- Phase 6D: ACTIVE.
- Phase 6D.1a App build-context gate: COMPLETE.
- Phase 6D.1b Docker image gate: COMPLETE.
- Phase 6D.1c Local App transfer bundle gate: COMPLETE.
- **Phase 6D.1: COMPLETE.**
- HA OS Local App first-start bootstrap: PASS.
- HA OS restart/health bootstrap: PASS.
- HA OS secret-safe token reuse + zero-runtime marker proof: PASS.
- **HA OS bootstrap gate: COMPLETE.**
- Next gate: Phase 6D.2 authenticated YI credential handoff.

## Live App-context proof

Development-machine smoke returned:

```text
python_compile=PASS
PHASE6D_APP_CONTEXT_PREPARE=PASS
app_context_prepare=PASS
required_app_artifacts=PASS
build_context_secret_scan=PASS
development_paths_removed=PASS
app_security_config=PASS
startup_policy_wiring=PASS
online_status_runtime_packaged=PASS
staged_python_compile=PASS
docker_build=SKIPPED
production_modified=false
PHASE6D_APP_CONTEXT_SMOKE=PASS
```

The staged context used the proven runtime at `.analysis/phase3/bionic-root`, but the generated App filesystem contains only fixed `/opt/yi-home` runtime paths. No `.env.local`, account credentials or persistent state were copied into the image context.

## Docker image proof

The real amd64 image gate then returned:

```text
docker_build=PASS
image_architecture=amd64
image_platform=linux/amd64
container_python_imports=PASS
container_runtime_dependencies=PASS
image_secret_state_scan=PASS
packaged_online_status_export=PASS
camera_sessions_started=false
production_go2rtc_ports_untouched=1984,8554
PHASE6D_DOCKER_IMAGE_SMOKE=PASS
```

This proves the packaged `linux/amd64` image contains and can execute/import the required runtime dependencies without starting `/run.sh` or opening YI sessions:

- Python + cryptography;
- FFmpeg + ffprobe;
- `qemu-aarch64`;
- go2rtc;
- staged Bionic linker/runtime;
- native online-status worker;
- export-capable `libPPPP_API.so` containing `PPPP_CheckDevOnline`;
- packaged backend Python modules.

The image filesystem scan found no `.env`, `.env.local`, backend API token, runtime policy or capability cache copied from development state.

## Local bundle path-gate correction

The first Local App bundle attempt stopped after the secret scan with:

```text
ERROR: development-machine path leaked into Local App package
```

This was a validation false positive, not an actual host-path leak. The staged `yi_runtime_lifecycle.py` retains a repo-relative development fallback `project_root/.analysis/phase3/bionic-root` for legacy development callers. The Home Assistant App never selects that fallback: `/run.sh` explicitly sets `RUNTIME_ROOT=/opt/yi-home/runtime/bionic-root` and passes both `--runtime-root` and the packaged `--worker-dir` to the backend.

The Local App bundle gate was corrected to:

- reject actual absolute development-host paths (`~/Documents`, `/home/<user>/Documents`) across packaged App source/config;
- reject `.analysis` in the real App startup files (`Dockerfile`, `run.sh`);
- require `/run.sh` to select `/opt/yi-home/runtime/bionic-root` explicitly;
- require the packaged Phase 3G worker directory to be passed explicitly.

No media/runtime implementation was changed for this correction.

## Local App transfer bundle proof

The corrected bundle gate passed live:

```text
PHASE6D_APP_CONTEXT_PREPARE=PASS
app_context_prepare=PASS
local_app_required_artifacts=PASS
local_supervisor_build_mode=PASS
local_app_secret_state_scan=PASS
local_app_development_paths_removed=PASS
local_app_runtime_wiring=PASS
local_app_bundle_secret_scan=PASS
production_modified=false
PHASE6D_LOCAL_APP_BUNDLE=PASS
```

Generated transfer artifact:

```text
dist/yi_home-local-amd64.tar.gz
```

Observed bundle size: `3920462` bytes. A companion SHA-256 file was generated at `dist/yi_home-local-amd64.tar.gz.sha256`.

The archive contains top-level `yi_home/`, so extracting it directly under `/addons` creates `/addons/yi_home/config.yaml`. The archive scan confirmed it contains no `.env`, `.env.local`, `yi.env`, backend API token, runtime policy or capability cache copied from development state.

## HA OS Local App bootstrap proof

The first HA OS start initially failed because host TCP `8554` was already in use by the existing production go2rtc/Frigate stack. The App was corrected so `8554/tcp` defaults to `null`: internal go2rtc still listens on 8554 inside the App network, but Supervisor no longer claims host port 8554 by default. Existing production media services were not changed.

After clearing the previously persisted host-port mapping in the App settings, YI Home started successfully under HA OS/Supervisor.

Observed first-start log:

```text
[03:48:56] INFO: Starting YI Home backend...
{"service":"yi-home-addon","api_version":"v1","bind":"0.0.0.0","port":8099,"authentication":"bearer","runtime_lifecycle_ready":true,"reprobe_ready":true,"media_publisher_enabled":true,"persistence_enabled":true,"initial_discovery_ok":false,"discovery_retry_enabled":true,"secrets_exposed":false}
[03:48:58] INFO: Published YI Home discovery information to Home Assistant.
[03:48:58] INFO: YI Home backend is ready.
```

Interpretation:

- Supervisor built and started the Local App successfully.
- Backend bearer-auth API is alive on internal TCP 8099.
- Runtime lifecycle, reprobe, managed publisher and persistence layers initialized successfully inside HA OS.
- `initial_discovery_ok=false` is expected before YI account credentials are configured.
- Background discovery retry is enabled.
- Supervisor `yi_home` discovery publication succeeded.
- No secrets were reported by the startup state.
- No host port 8554 takeover is required for core App operation.

This is the first proof that the packaged engine boots cleanly under real HA OS/Supervisor rather than development Docker infrastructure.

## HA OS restart/health proof

The App was then restarted under Supervisor. The log shows the original healthy instance shutting down through s6 and a fresh instance reaching the same ready state:

```text
[03:50:57] INFO: Starting YI Home backend...
{"service":"yi-home-addon","api_version":"v1","bind":"0.0.0.0","port":8099,"authentication":"bearer","runtime_lifecycle_ready":true,"reprobe_ready":true,"media_publisher_enabled":true,"persistence_enabled":true,"initial_discovery_ok":false,"discovery_retry_enabled":true,"secrets_exposed":false}
[03:50:57] INFO: Published YI Home discovery information to Home Assistant.
[03:50:57] INFO: YI Home backend is ready.
```

This proves restart health and clean Supervisor re-entry with empty YI account configuration.

The current Home Assistant CLI also reports that `ha addons` is deprecated in favor of `ha apps`; all follow-up HA OS commands should use `ha apps`.

## Secret-safe persistence proof

The App was rebuilt with explicit persistence instrumentation and restarted on HA OS. The live result was:

```text
[03:57:01] INFO: Backend API token reused; mode=600; value_exposed=false.
[03:57:01] INFO: Starting YI Home backend...
{"service":"yi-home-addon","api_version":"v1","bind":"0.0.0.0","port":8099,"authentication":"bearer","runtime_lifecycle_ready":true,"reprobe_ready":true,"media_publisher_enabled":true,"persistence_enabled":true,"managed_runtime_count":0,"initial_discovery_ok":false,"discovery_retry_enabled":true,"secrets_exposed":false}
[03:57:01] INFO: Published YI Home discovery information to Home Assistant.
[03:57:01] INFO: YI Home backend is ready.
```

This closes the HA OS bootstrap persistence gate:

- `/data/backend-api-token` survived the App rebuild/restart and was reused;
- token permissions were mode `0600`;
- the token value was never logged;
- zero camera runtimes were restored from the empty runtime policy;
- backend health, persistence, media publisher initialization and Supervisor discovery all returned after restart;
- account discovery remains intentionally pending because no YI credentials have been handed to the App yet.

## Exit decision

**Phase 6D.1 and the HA OS bootstrap gate are COMPLETE.** Phase 6D remains ACTIVE. The next implementation gate is Phase 6D.2: authenticated Integration → App credential handoff, persistent secret storage under `/data`, live YI account validation/discovery, and secret-safe account status. Do not mark Phase 6D complete until configured-account discovery/availability, media streaming and configured-account restart behavior pass.
